ZMFH is a Python import governance layer that safely observes and controls module imports.
It uses fail-open by default and supports strict, policy-based blocking, including installed packages.
ZMFH adds clear diagnostics, automatic bootstrapping, and CLI tools for secure, reproducible Python projects